<h2>Encyclopedia Data Linked to Me</h2>

<% unless @hometown.blank? %>
	<h3>My hometown: <%= @hometown['name'] %></h3>
	
	<p>
		<%= @hometown_notable_people['total_number'] %> notable <%= @hometown_notable_people['total_number'] == 1 ? 'person was' : 'people were' %> born in your hometown...
		<% if @hometown_notable_people['total_number'] > 10 %>
			<br/>(Showing 10 randomly selected people)
		<% end %>
	</p>

	<% unless @hometown_notable_people['notable_people'].blank? %>

		<table class='linked_data_table'>
			<tr>
				<th>Name</th>
				<th>DOB</th>
			</tr>

			<% @hometown_notable_people['notable_people'].each do |person| %>

				<tr>
					<td><%= link_to person.name, person.resource_id, {'target'=>'_blank'} %></td>
					<td>
						<% unless person.birthday.blank? %>
							<%= person.birthday.strftime('%B %d, %Y') %>
						<% end %>
					</td>
				</tr>

			<% end %>

		</table>
	<% end %>
	
<% end %>


<% unless @current_location.blank? %>
	<h3>My current location: <%= @current_location['name'] %></h3>
	
	<p>
		<%= @current_location_notable_people['total_number'] %> notable <%= @current_location_notable_people['total_number'] == 1 ? 'person was' : 'people were' %> born in your current location...
		<% if @current_location_notable_people['total_number'] > 10 %>
			<br/>(Showing 10 randomly selected people)
		<% end %>
	</p>

	<% unless @current_location_notable_people['notable_people'].blank? %>

		<table class='linked_data_table'>
			<tr>
				<th>Name</th>
				<th>DOB</th>
			</tr>

			<% @current_location_notable_people['notable_people'].each do |person| %>

				<tr>
					<td><%= link_to person.name, person.resource_id, {'target'=>'_blank'} %></td>
					<td>
						<% unless person.birthday.blank? %>
							<%= person.birthday.strftime('%B %d, %Y') %>
						<% end %>
					</td>
				</tr>

			<% end %>

		</table>
	<% end %>
	
<% end %>
